# Fixture Data

Reusable example datasets for the MVP belong here when they are shared outside the test suite.

Phase 2 will add deterministic annual Tesla-shaped fixture data under either this directory or `tests/fixtures/`.
